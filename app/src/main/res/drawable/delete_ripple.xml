<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Customize the ripple color if needed -->

    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" /> <!-- Customize the mask color if needed -->
        </shape>
    </item>

    <item>
        <bitmap android:src="@drawable/delete" android:gravity="center" />
    </item>

</ripple>